# שנה את שורת root ל:server { root /usr/share/nginx/html; listen 80;
    server_name _;
# הפעל מחדש את nginx
sudo systemctl restart nginx    location / {
        root /home/ec2-user/checkers-game/client;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /socket.io/ {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }
}
